!!!
%html
  %head
    %title= page_title

    = stylesheet_link_tag :all

    = javascript_include_tag 'jquery/jquery-1.4.2.min'
    = javascript_include_tag :all, :cache => true

    = csrf_meta_tag

  %body
    #wrapper
      .box_full
        %p.listen= link_to "Listen", "/player", :target => Track.window_name
        .clear

      #header
        = image_tag "http://placehold.it/650x130"
      #nav
        = render :partial => 'shared/nav'
        = render :partial => 'shared/admin_nav' if controller.current_user
      #content
        .minheight
        .main
          = render :partial => "shared/flashes"
          = yield
        .minheight_clear
      #footer
        = image_tag "http://placehold.it/650x50"
        
      .box_full
        %p.credits
          &copy; #{Time.now.year}
          Grace Hrabi
          and
          Kevin McPhillips
