%h1 Edit event

= render 'form'

.actions
  = index_entity_image admin_events_path, "Back to Events Index"
  = destroy_entity_image admin_event_path(@event), "Delete Event"
  
- if @preview
  .preview
    %h2 Preview

  = render :partial => '/events/event', :object => @event
