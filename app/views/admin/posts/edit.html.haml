%h1 Edit Blog Post

= render 'form'

.actions
  = index_entity_image admin_posts_path, "Back to Blog Index"
  = destroy_entity_image admin_post_path(@post), "Delete Blog Post"

- if @preview
  .preview
    %h2 Preview

  = render :partial => '/posts/post', :object => @post
    