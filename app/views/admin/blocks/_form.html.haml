= form_for([:admin, @block]) do |f|
  = error_messages(@block)

  .field
    = f.label :body
    .input= f.text_area :body, rows: 16, cols: 60

  - begin
    = render @block.label
  - rescue ActionView::MissingTemplate

  = f.image if @block.accepts_image?

  .actions
    %label Actions
    .input= f.submit "Save"
