<%= sidebar_sections :audio_player, :events, :gallery %>

<div class="content">
  <% @posts.each do |post| %>
    <%= render :partial => "post", :object => post %>
  <% end %>
  <div class="post_pagination">
    <%= paginate(@posts) %>
  </div>
</div>
